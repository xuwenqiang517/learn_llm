# learn_llm - 大模型学习项目依赖
# 基于阿里巴巴编码规范，使用稳定的版本范围

# 核心框架
torch>=2.0.0,<3.0.0
transformers>=4.35.0,<5.0.0
datasets>=2.14.0,<3.0.0
accelerate>=0.25.0,<1.0.0
peft>=0.7.0,<1.0.0

# 数值计算与可视化
numpy>=1.24.0,<2.0.0
matplotlib>=3.7.0,<4.0.0

# API服务
fastapi>=0.109.0,<1.0.0
uvicorn>=0.27.0,<1.0.0
pydantic>=2.5.0,<3.0.0
python-multipart>=0.0.6,<1.0.0

# LangChain 框架
langchain>=0.2.0,<1.0.0
langchain-ollama>=0.1.0,<1.0.0

# 金融数据
akshare>=1.11.0,<2.0.0
tabulate>=0.9.0,<1.0.0

# 缓存与消息队列
redis>=5.0.0,<6.0.0

# 监控与指标
prometheus-client>=0.19.0,<1.0.0

# 评估指标
nltk>=3.8.0,<4.0.0
tqdm>=4.66.0,<5.0.0

# 开发工具（可选）
black>=23.0.0,<24.0.0
flake8>=7.0.0,<8.0.0
mypy>=1.8.0,<2.0.0
pytest>=8.0.0,<9.0.0
pytest-cov>=4.1.0,<5.0.0

# 文档生成（可选）
sphinx>=7.0.0,<8.0.0
sphinx-rtd-theme>=2.0.0,<3.0.0

# 量化工具（可选）
bitsandbytes>=0.41.0,<1.0.0
optimum>=1.16.0,<2.0.0

# 推理优化（可选）
tensorrt>=8.6.0,<9.0.0
onnx>=1.15.0,<2.0.0
onnxruntime>=1.17.0,<2.0.0

# Jupyter Notebook支持（可选）
ipython>=8.0.0,<9.0.0
ipykernel>=6.0.0,<7.0.0
notebook>=7.0.0,<8.0.0

# 依赖版本约束说明：
# 1. torch系列：保持2.x版本以支持最新特性
# 2. transformers：4.35+支持最新的模型架构
# 3. peft：0.7+支持更多高效微调方法
# 4. 避免使用^或~=版本语法，保持pip兼容性
# 5. 可选依赖按需安装，不强制要求
